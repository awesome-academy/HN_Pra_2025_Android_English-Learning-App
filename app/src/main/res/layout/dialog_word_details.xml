<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/dp_24"
    android:background="@drawable/bg_rounded_card">

    <!-- Word with sound icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="@dimen/dp_16">

        <TextView
            android:id="@+id/tv_word_popup"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="example"
            android:textSize="24sp"
            android:textColor="@color/black"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/iv_sound_popup"
            android:layout_width="@dimen/dp_32"
            android:layout_height="@dimen/dp_32"
            android:src="@drawable/ic_sound"
            android:background="@drawable/bg_circle_gray"
            android:padding="@dimen/dp_8"
            app:tint="#666666" />

    </LinearLayout>

    <!-- Part of speech -->
    <TextView
        android:id="@+id/tv_part_of_speech_popup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="noun"
        android:textSize="14sp"
        android:textColor="#666666"
        android:textStyle="italic"
        android:layout_marginBottom="@dimen/dp_12"
        android:background="@drawable/bg_part_of_speech_modern"
        android:paddingStart="@dimen/dp_8"
        android:paddingEnd="@dimen/dp_8"
        android:paddingTop="@dimen/dp_4"
        android:paddingBottom="@dimen/dp_4" />

    <!-- Definition -->
    <TextView
        android:id="@+id/tv_definition_popup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="A thing that is or is likely to be wrongly perceived or interpreted by the senses."
        android:textSize="16sp"
        android:textColor="@color/black"
        android:lineSpacingExtra="@dimen/dp_4"
        android:layout_marginBottom="@dimen/dp_16" />

    <!-- Example (if available) -->
    <TextView
        android:id="@+id/tv_example_popup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Example: The mirage was just an optical illusion."
        android:textSize="14sp"
        android:textColor="#666666"
        android:textStyle="italic"
        android:background="@drawable/bg_example_modern"
        android:padding="@dimen/dp_12"
        android:visibility="gone" />

    <!-- Close button -->
    <Button
        android:id="@+id/btn_close_popup"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dp_36"
        android:layout_gravity="center"
        android:text="@string/close"
        android:textColor="@color/white"
        android:background="@drawable/bg_blue_button"
        android:paddingStart="@dimen/dp_24"
        android:paddingEnd="@dimen/dp_24"
        android:layout_marginTop="@dimen/dp_16" />

</LinearLayout>